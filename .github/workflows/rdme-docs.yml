# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-12-20T00:05:27.815Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ReadMe GitHub Action 🦉

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `rp-core-newdocs-dec23-readme`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - rp-core-newdocs-dec23-readme

jobs:
  rdme-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `docs` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: docs doc/source --key=${{ secrets.README_API_KEY }} --version=1.0 --dryRun
